
export interface GeneratedImage {
  src: string;
}

// FIX: Define and export the ModelType for use in ModelSelector and App components.
export type ModelType = 'gemini-2.5-flash-image' | 'imagen-4.0-generate-001';
